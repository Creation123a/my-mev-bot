{
  "$schema": "https://railway.com",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go mod download && go mod tidy && go build -ldflags='-w -s' -o out ."
  },
  "deploy": {
    "startCommand": "./out"
  }
}
