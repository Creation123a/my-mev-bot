{
  "$schema": "https://railway.com",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "ln -sf config Bot/Config && go mod tidy && go build -o out ."
  },
  "deploy": {
    "startCommand": "./out",
    "restartPolicyOnFailure": "ON_FAILURE"
  }
}
